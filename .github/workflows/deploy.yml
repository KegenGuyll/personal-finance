name: Deploy

# Pushes to `main` build, push to GHCR, and notify the home-server deploy agent.
on:
  push:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  deploy:
    uses: KegenGuyll/personal-pipeline/.github/workflows/deploy-service.yml@main
    with:
      service: personal-finance
    secrets:
      deploy_webhook_url: ${{ secrets.DEPLOY_WEBHOOK_URL }}
      deploy_webhook_secret: ${{ secrets.DEPLOY_WEBHOOK_SECRET }}
      service_env: ${{ secrets.SERVICE_ENV }}
